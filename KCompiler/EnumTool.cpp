#include "EnumTool.h"
template <class E>
std::string EnumTool<E>::GetString(E e)
{
	if (is_enum(E))
	{
		return 
	}
	cerr << "this class isn't enum class." << std::endl;
	return "";
}
template <class E>
E EnumTool<E>::GetEnum(std::string e)
{
	if (is_enum(E))
	{

	}
	cerr << "this class isn't enum class." << std::endl;
	return "";
}